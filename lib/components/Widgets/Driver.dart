import 'package:flutter/material.dart';
class drivere extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

      return Container(
        height: 1,
        margin: EdgeInsets.all( 3),
        color: Colors.black26,
      );

  }
}
